# Port targets — `rcgen-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `rcgen` that codex-rs uses, and therefore the minimum subset that `rcgen-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-116] (rank `66`, tier `direct`, status `To Do`)

**Cargo package name**: `rcgen`
**Rust module name (in `use` statements)**: `rcgen`

## Summary

- **10** distinct symbols imported by codex-rs
- **10** total `use` statements
- **1** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `rcgen::BasicConstraints`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `codex-client/tests/ca_env.rs:12` (`codex-client`) — `use rcgen::BasicConstraints`

### `rcgen::CertificateParams`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `codex-client/tests/ca_env.rs:13` (`codex-client`) — `use rcgen::CertificateParams`

### `rcgen::CertifiedIssuer`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `codex-client/tests/ca_env.rs:14` (`codex-client`) — `use rcgen::CertifiedIssuer`

### `rcgen::DistinguishedName`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `codex-client/tests/ca_env.rs:15` (`codex-client`) — `use rcgen::DistinguishedName`

### `rcgen::DnType`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `codex-client/tests/ca_env.rs:16` (`codex-client`) — `use rcgen::DnType`

### `rcgen::ExtendedKeyUsagePurpose`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `codex-client/tests/ca_env.rs:17` (`codex-client`) — `use rcgen::ExtendedKeyUsagePurpose`

### `rcgen::IsCa`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `codex-client/tests/ca_env.rs:18` (`codex-client`) — `use rcgen::IsCa`

### `rcgen::KeyPair`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `codex-client/tests/ca_env.rs:19` (`codex-client`) — `use rcgen::KeyPair`

### `rcgen::KeyUsagePurpose`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `codex-client/tests/ca_env.rs:20` (`codex-client`) — `use rcgen::KeyUsagePurpose`

### `rcgen::PKCS_ECDSA_P256_SHA256`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `codex-client/tests/ca_env.rs:21` (`codex-client`) — `use rcgen::PKCS_ECDSA_P256_SHA256`
